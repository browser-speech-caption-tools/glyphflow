# GlyphFlow documentation site

This is the Docusaurus site for the GlyphFlow API and browser support notes.

```sh
npm install
npm run start
```

To preview Japanese locally, run:

```sh
npm run start -- --locale ja
```

Docusaurus development serves one locale at a time. The production build includes both English and Japanese.
